apiVersion: 1

providers:
  - name: minillm
    orgId: 1
    folder: miniLLM
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
